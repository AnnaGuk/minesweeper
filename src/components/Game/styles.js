import styled from "styled-components";
import { Toolbar, List } from "react95";

export const StyledToolbar = styled(Toolbar)`
  justify-content: space-between;
`;

export const StyledList = styled(List)`
  position: absolute;
  left: 0;
  top: 100%;
`;
